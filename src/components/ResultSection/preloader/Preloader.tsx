import preloader from '../../../img/loader.svg'

const Preloader = () => {
    return (
        <div>
            <img src={preloader}
    alt="preloader" style={{
    width: '25%'}
            }/>
        </div>
)
};

export default Preloader;